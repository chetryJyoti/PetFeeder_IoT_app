import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Pet_feeder_app',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
