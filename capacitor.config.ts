import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.Chetry',
  appName: 'PetFeeder',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
